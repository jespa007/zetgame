#include "zg_graphics.h"
/*
void 		Shape2dChannel_Reset(ChannelsInfo *channels){

	if(channels == NULL) return;

	// reset to 0...
	memset(channels->result,0,channels->n_channels*sizeof(float));
}
*/
