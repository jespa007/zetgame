#include "zg_graphics.h"

/*
Shape2dAction * Shape2dAction_New(void){

	Shape2dAction *shape2d_action = NEW(Shape2dAction);
	shape2d_action->action=Action_New(SHAPE2D_CHANNEL_MAX);
	shape2d_action->action->channels->reset=Shape2dChannel_Reset;

	return shape2d_action;
}

void Shape2dAction_SetKeyFramesIndex(Shape2dAction *_this,uint8_t channel_mask, const char * ease,const float * points, size_t points_len){
	Action_SetKeyframes(_this->action,SHAPE2D_CHANNEL_SELECTED_INDEX,channel_mask,ease,points,points_len);
}


void Shape2dAction_Delete(Shape2dAction *_this){
	if(_this == NULL) return;

	Action_Delete(_this->action);
}


*/
